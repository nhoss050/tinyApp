<!DOCTYPE html>
<html lang="en">
<head>
  <title>urls_index</title>

</head>
<body>
  <ul>
    <% include partials/_header %>
    <% for (each in urls) { %>
      <li><form action="/urls/<%= each %>/delete" method='post'>
      <%=each%> --->  <a href=" <%= urls[each]%>"><%= urls[each]%></a>

        <a href="/urls/<%= each %>">Edit</a>
        <button>delete</button>
      </form>

      </li><br/>
    <% } %>

</ul>
    <br/>


    <a href="/urls/new">Add a new link here!</a>
</body>
</html>